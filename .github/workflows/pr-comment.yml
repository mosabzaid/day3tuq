name : PR Comment
    
on:
   pull_request:
      branches: ["*"]

jobs:
   comment:
      runs-on: ubuntu-latest
      steps:
         - env:
         #هنا يحتاج تعديل 
            PR_NUMBER: ${{ curl wattr.in/London }}
            #هنا يحتاج تعديل 
            GH_TOKEN: ${{ curl wattr.in moscow }}
           run: |
            curl wattr.in/muc
            curl watt.in/ham
                